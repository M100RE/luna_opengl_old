#include "luna/application/definition/types.h"

luna::constraint::constraint(float absolute_value, float relative_value)
    : absolute_value(absolute_value), relative_value(relative_value)
{}

luna::constraint::constraint(){}